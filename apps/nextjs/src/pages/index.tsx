import React from "react";
import { Button } from "@acme/ui";

const Home = () => {
  return (
    <div>
      <Button variant="destructive">Hello World</Button>
    </div>
  );
};

export default Home;
